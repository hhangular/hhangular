---
## L'attribut `borderWidth`

L'attribut `borderWidth` définit simplement la largeur de la bordure autour de chaque miniature.

Cette bordure est nécessaire car c'est celle ci qui change d'aspect lors de la sélection de ces dernières.

Celle ci devrait être en relation avec l'attribut `fitSize`.
