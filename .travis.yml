language: node_js
node_js:
  - "10"
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
cache:
  directories:
    - "node_modules"
branches:
  except:
    - master
before_script:
  - git checkout $TRAVIS_BRANCH
  - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/.npmrc
  - npm install
  - bash scripts/test-libs.sh
script:
  - bash scripts/build-all.sh
deploy:
  - provider: script
    script: bash scripts/deploy-on-npm.sh $TRAVIS_BRANCH
    skip_cleanup: true
    on:
      branch:
        - pdfjs
        - store
  - provider: script
    script: git commit -m "[ci skip]" package-lock.json; npm version patch -m "[ci skip]"
    skip_cleanup: true
    on:
      branch:
        - pdfjs
        - store
        - website
  - provider: script
    script: bash scripts/push-on-master.sh
    on:
      branch:
        - pdfjs
        - store
        - website
  - provider: releases
    user: "$GITHUB_USER"
    api_key: "$GITHUB_TOKEN"
    file_glob: true
    file: dist/*
    skip_cleanup: true
    on:
      branch:
        - pdfjs
        - store
        - website
  - provider: pages
    skip-cleanup: true
    github-token: "$GITHUB_TOKEN"
    local-dir: "dist/hhangular"
    on:
      branch:
        - pdfjs
        - store
        - website
